1、前端-用户看见
2、后台-数据、接口
3、后台管理-管理员看的


知识点：
1、数据交互
2、数据库
3、Node:框架(Express,Koa)、接口、身份认证、服务端渲染
4、后台管理：增删改查、文件操作

--------------------------------------------------

项目
1、数据字典-哪些域
2、接口-哪些接口、哪些数据
3、前台库-原生+jquery
4、后台库-express、koa
5、后台管理系统:bootstrap
6、数据库:MySQL、Oracle、MongoDB、Redis

-------------------------------------------------
Node.js
1、接口
2、数据处理（get，post，file）
3、身份验证（cookie，session）

----------------------------
后台管理系统
1、布局
2、功能

----------------------------

前台展现

-------------------------------------------------

原生Node--第二十四课node知识
1、数据处理、文件上传
2、压缩		zlib
3、管道操作	stream
4、接口管理	Router实现

*stream-读一块、发一块
1、读取流
2、写入流
3、读写流-压缩流

*zlib
zlib.createGzip();

-------------------------------------------------
当前接触：
读取流：require(),fs.createReadStream();
写入流：res,fs.createWriteStream();
读写流：zlib.createGzip();

-------------------------------------------------

1、压缩文件
2、压缩网络数据+res.setHeader('Content-Encoding','gzip')  --否则浏览器会误会成为二进制

--------------------------------------------------

setHeader和writeHeader区别：
setHeader 可以设置无数次
writeHeader 发送数据前设置的（只能一次）

--------------------------------------------------
路由(Router)
-----------------------------------
EventEmitter-事件队列

	const EventEmitter = require('events');
	
	class MyEmitter extends EventEmitter {}
	
	const myEmitter = new MyEmitter();
	myEmitter.on('event', () => {
	  console.log('触发事件');
	});
	myEmitter.emit('event');

-----------------------------------
node模块
所有的模块是单例模式（文件不管引几次都是一样的）；

-----------------------------------
